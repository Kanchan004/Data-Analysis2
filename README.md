## This repo consists the basic regression task in which the dataset is trained with Linear regression model.
## Firstly libraries such as pandas and sklearn are imported.
## Then the dataset is loaded; dataset (stoc prices).
## Two targeted columns are taken which are 'open' and 'high'.
## Then train-test splitting is done later model is trained. 
## Prediction is measured by evaluation matrices such as mse and r2 score.
